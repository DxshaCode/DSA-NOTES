//problem: middle of the linked list
//concept: hare and tortoise algo. 
        //it has two pointers one fast and one slow.slow moves one step and fast moves 2 steps.

//approach: when fast pointer moves to the end slow pointer will be  at midlle so in one traversal only we can find the middle node of a linked list.

//time complexity: O(n);
//space complexity: O(1)

